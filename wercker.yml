box: jacekmarchwicki/android
dev:
  steps:
    - script:
      name: run gradle
      code: |
        ./gradlew bootRun
build:
  steps:
    - script:
        name: run gradle
        code: |
          ./gradlew build
