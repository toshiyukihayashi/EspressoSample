box: java 8
dev:
  steps:
    - script:
      name: run gradle
      code: |
        ./gradlew bootRun
build:
  steps:
    - script:
        name: run gradle
        code: |
          ./gradlew build
