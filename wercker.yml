box: java
dev:
  steps:
    - script:
      name: run gradle
      code: |
        ./gradlew bootRun
build:
  steps:
    - script:
        name: run gradle
        code: |
          ./gradlew --full-stacktrace -q --project-cache-dir=$WERCKER_CACHE_DIR build
